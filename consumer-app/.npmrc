# Prefer packing sabik-datagrid (`npm pack` at repo root) over `file:..`.
# A directory install junctions the library repo and can expose a second React copy.
# Kept for future file: directory installs where npm honors this flag.
install-links=false
